using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 어빌리티 등장 확률을 가지고 있기 위한 클래스입니다.
[CreateAssetMenu(fileName = "AbilityProbability", menuName = "ScriptableObject/AbilityReroll", order = 0)]
[Serializable]
public class AbilityRerollProbability : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
